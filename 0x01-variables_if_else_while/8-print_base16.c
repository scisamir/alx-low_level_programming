#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	for (int i = 48; i <= 57; i++)
		putchar(i);
	for (int j = 97; j <= 102; j++)
		putchar(j);
	putchar('\n');

	return (0);
}
